<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="550"
             Background="Black"
             xmlns:vm="using:Jaguar.Desktop.ViewModels" 
             x:DataType="vm:WorkflowViewModel"    
             x:Class="Jaguar.Desktop.Views.WorkflowView">

    <!-- <DockPanel> -->
    <!--     ~1~ <Canvas Width="400" Background="#282828"> @1@ -->
    <!--     ~1~     <Border Width="200" Height="50" Background="Azure" CornerRadius="10" Margin="30, 40"> @1@ -->
    <!--     ~1~         <TextBox Text="{Binding AgentSelectionStatus}" Name="{Binding AgentSelectionStatus}"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="1000"></TextBox> @1@ -->
    <!--     ~1~     </Border> @1@ -->
    <!--     ~1~     <Button Command="{Binding SetAgentStatus}"></Button> @1@ -->
    <!--     ~1~ </Canvas> @1@ -->
    <!-- </DockPanel> -->
    <!-- <TextBlock Text="{Binding ViewHeader}"  -->
    <!--            Margin="10"  -->
    <!--            FontSize="18"/> -->
    kkkkkkkkkkkkkkkkkkkkkkkk
</UserControl>
